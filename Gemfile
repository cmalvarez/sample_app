source 'http://rubygems.org'

  gem 'rails', '3.0.9'
  gem 'sqlite3', '1.3.3'
  gem 'gravatar_image_tag', '1.0.0.pre2'  
  gem 'will_paginate', '3.0.pre2'

  group :development do
    gem 'rspec-rails', '2.6.1'
    gem 'annotate', '2.4.0'    
    gem 'faker', '0.3.1'
  end

  # para acceder a los generadores RSpec-specific 

  group :test do
    gem 'rspec-rails', '2.6.1'
    gem 'webrat', '0.7.1'
    gem 'factory_girl_rails', '1.0'
  end

# las versiones aquí vienen del libro en linea cap. 3
# rspec es para correr los tests
# webrat antes se instalaba automaticamente, ahora hay que indicarlo
# esto cambia de acuerdo al libro online que tiene las versiones más actualizadas. Y también cambia según lo que haya que irle agregando en cada capítulo.

